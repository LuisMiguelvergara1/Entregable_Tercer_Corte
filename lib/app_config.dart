
// Ubicación: lib/app_config.dart

// Claves PÚBLICAS (Sin guion bajo para que sean accesibles desde la carpeta screens)
const String userEmailKey = 'userEmail';
const String userPasswordKey = 'userPassword';
const String userRoleKey = 'userRole';

// Definición de Roles
enum UserRole { user, admin }